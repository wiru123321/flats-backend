spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/zarzadzanie_osiedlem
    username: postgres
    password: admin
    initialization-mode: always
server:
  port: 8080